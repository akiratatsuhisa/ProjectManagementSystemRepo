@model StatisticsViewModel
@{
    ViewData["Title"] = Localizer["Statistics"];
}

<h1>Statistics</h1>

<form asp-action="Statistics" method="get">

    <div class="form-group">
        <label>Status</label>
        <select class="custom-select" multiple name="Statuses" asp-items="@ViewBag.Statuses" asp-for="Statuses">
            <option value="">--None--</option>
        </select>
    </div>

    <div class="form-group">
        <button class="btn btn-primary">Submit</button>
    </div>
</form>

